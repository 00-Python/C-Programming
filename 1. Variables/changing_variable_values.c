#include <stdio.h>

int main() {
    int amount = 5;
    printf("I am a %dx Programmer now i can do a Hello World in C!\n", amount);

    printf("Let's change a variable's value to level up!\n");

    amount = 10;
    printf("Now I'am a %dx Programmer now change the value of variable!", amount);
    return 0;
}